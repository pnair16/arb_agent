# Arbitrage Scanner MVP — Python dependencies
# Install with: pip install -r requirements.txt

# Async HTTP client
aiohttp>=3.9.0

# Fuzzy string matching (entity resolution for event names)
thefuzz>=0.22.1
# Optional: C-extension speedup for thefuzz (highly recommended for large datasets)
python-Levenshtein>=0.25.0
